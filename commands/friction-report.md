---
description: Show where exit-status-guard friction accumulates, from your local Claude Code session transcripts.
argument-hint: "[--since 24h] [--repo name] [--raw] [--json] [--top N]"
allowed-tools: Bash
---

The exit-status-guard friction report below is generated by re-reading the hook
decisions Claude Code already recorded in your local session transcripts (it adds
no telemetry — see PRIVACY.md). Show it to me verbatim, then add at most one
sentence naming the rule that fires most and whether it looks like a habit worth
fixing or a defect in the registry.

Every exit-status-guard verdict is a deny, and there is no denominator: the
guard emits nothing when it has no objection, so silent runs leave no record.
Read the counts as "false greens caught", not as a friction rate. Do not edit
any files.

!`python3 "${CLAUDE_PLUGIN_ROOT}/scripts/friction-report.py" $ARGUMENTS`
