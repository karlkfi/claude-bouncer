---
id: Q86
rank: ac
labels:
    - feature
    - foreground-guard
    - open-question
status: ready
size: S
---

# Decide whether `auto` still belongs in `UNATTENDED_MODES`

Now both classes deny by default, so the set only fires on a config-de-escalated `ask` — where a repo asked to be prompted. `dontAsk`/`bypassPermissions` are clear; `auto` is the question.
