---
id: Q101
rank: ahi
labels:
    - docs
    - workspace-guard
status: ready
size: S
---

# The README still says an `ask` blocks in every mode

Q79 corrected `docs/permission-modes.md`, which had claimed `ask` and `deny`
block in every mode — false on a literal read, because an `ask` the operator
approves runs. The README repeats it:
`plugins/workspace-guard/README.md:1581:block in every mode, but a`. Its own
preceding bullet already carries the precise form ("in non-interactive /
headless runs there is no one to answer an `ask` prompt"), so this is a one-line
tightening to match, not a re-measurement. Filed rather than fixed alongside
Q79 because another session in that batch held the README.
