---
id: Q93
rank: ae
labels:
    - repo
    - docs
status: ready
size: S
---

# Publish the backlog index, which a directory listing does not give

`docs/queue/` sorts Q10 before Q2 on github.com, so a reader browsing the repo has no priority order; `make backlog` covers a local checkout only, and the rendered table must never be committed — it is the one file every completing session would edit. Measured 2026-08-22: the repo is private (`gh repo view --json visibility`) with no Pages site (`gh api …/pages` returns 404), so Pages needs a paid plan. That leaves the CI job summary and a gitignored local page.
