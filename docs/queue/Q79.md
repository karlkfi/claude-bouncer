---
id: Q79
rank: ag
labels:
    - docs
    - retro
    - workspace-guard
status: ready
size: S
target: ../../plugins/workspace-guard/docs/permission-modes.md
---

# Correct or re-measure permission-modes.md on what an `ask` blocks

Measured 2026-08-18: an `ask` the operator approves runs, so "block everywhere" is false on a literal read. The v1.10.0 notes cite it as why Q72 checks the path outright, so it is load-bearing. Reword, or re-run the forced-decision measurement.
