---
id: Q80
rank: a2
labels:
    - security
    - workspace-guard
status: ready
size: S
---

# Guard `ln`, which creates a link to any path unchecked

Measured 2026-08-19: `ln -s /q167-src /q167-link` defers, while `cp` to the same path denies. `classify_ln` already parses the operands to stage links in a chain; nothing checks them.
