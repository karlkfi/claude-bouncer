---
id: Q94
rank: ahi
labels:
    - docs
    - prod-guard
    - workspace-guard
status: ready
size: S
---

# Archive the plan docs whose items have already shipped

Found during the backlog consolidation. Every file in
[`plugins/prod-guard/docs/plan/`](../../plugins/prod-guard/docs/plan) belongs to
an item that has shipped — its closing checklist names a Q-ID no longer in the
store, and the plugin has one live item, which has no plan doc.
`plugins/workspace-guard/docs/plan/q70-parallel-tests.md` is the same shape.
Both trees already have an `archive/` subdirectory, so this is `git mv` plus a
read of each file to confirm nothing live still points at it.
