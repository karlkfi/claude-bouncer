---
id: Q82
rank: ab
labels:
    - tests
    - docs
    - workspace-guard
status: ready
size: S
---

# Reconcile the "no real sensitive paths in fixtures" rule with the tree

Fixtures must name synthetic placeholders, not real sensitive paths.
Re-measured 2026-08-22 over
`plugins/workspace-guard/tests/test_workspace_guard.py`: `/etc/passwd` appears
35 times, `id_rsa` and `.aws/credentials` 6 times each. Rename, or narrow the
rule.
