---
id: Q82
rank: ab
labels:
    - tests
    - docs
    - workspace-guard
status: ready
size: S
---

# Reconcile the "no real sensitive paths in fixtures" rule with the tree

Fixtures must name synthetic placeholders, not real sensitive paths. `tests/test_workspace_guard.py` uses `/etc/passwd` ~20x, `~/.ssh/id_rsa`, `$HOME/.aws/credentials`. Rename, or narrow the rule.
