---
id: Q47
rank: ak
labels:
    - security
    - workspace-guard
status: deferred
size: M
---

# Catch a `**` glob item that matches fewer segments than the pattern

**Demand:** a session runs `shopt -s globstar`. Verified: `docs/**` expands to `docs/` too, so a loop body's trailing `../` climbs above the root undetected. Issue 99's proxy needs fixed segments.
