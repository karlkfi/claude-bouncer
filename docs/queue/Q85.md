---
id: Q85
rank: af
labels:
    - infra
    - workspace-guard
status: ready
size: S
---

# Count the friction report's native-tool decisions

Only Bash reaches them today. Both passes filter on `PreToolUse:Bash` ([scope](../../plugins/workspace-guard/docs/development/measuring-friction.md)), so a guarded `Edit`/`Write`/`Read` is absent, sibling-checkout denies included. Measured 2026-08-21: 5 such blocks beside 80 Bash ones.
