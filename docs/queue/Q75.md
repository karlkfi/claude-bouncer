---
id: Q75
rank: a0
labels:
    - security
    - workspace-guard
status: ready
size: S
---

# Guard `cut` and `base64`, which read outside the root under an `allow`

Measured: `cat in.txt && cut -f1 /outside` returns `allow`, while `xxd`/`od`/`strings`/`wc` already ask. `cut` appears 1,090 times in the corpus. Add the two `SPEC` rows.
