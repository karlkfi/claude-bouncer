---
id: Q54
rank: am
labels:
    - parsing
    - workspace-guard
status: deferred
size: S
---

# Quiet `$_` in a PowerShell `ForEach-Object` block

**Demand:** the friction report shows `$_` prompts accumulating. It reports as 'expand' like bash's `cat $f`, but carries far more of PowerShell's idiom. See [Q51's known gaps](../../plugins/workspace-guard/docs/plan/q51-powershell-tool.md).
